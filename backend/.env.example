A4F_BASE_URL=https://api.a4f.co/v1
A4F_CHAT_MODEL=provider-6/gpt-4o           # higher quality + larger context
A4F_VISION_MODEL=provider-6/gpt-4o         # use when OCR fails / for handwriting & diagrams
A4F_EMBED_MODEL=provider-3/text-embedding-3-small
USE_LOCAL_OCR=1                              # keep local OCR as first-pass, fallback to vision if needed
RAG_DB=rag_db.json
